/**
 * Placeholder for future git helpers.
 *
 * Planned helpers must use pi.exec("git", args) with argv arrays and must not
 * stage files, create commits, or edit working-tree files.
 */
export {};
